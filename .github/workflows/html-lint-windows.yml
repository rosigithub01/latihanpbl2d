name: Cek Kode HTML(Windows)

on:
  push:
    branches:
    - main
  pull request:
jobs:
  lint: rusn-on:windows-latest

  step: 
  - name: Checkout Repository
  uses: action/checkout@v4

  - name: Install HTMLHint
  run: npm install -g htmlhint

  - name: Periksa HTML
  run: htmlhint"**/*.html"
